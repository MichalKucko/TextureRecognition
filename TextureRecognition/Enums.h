#pragma once

/**
* image preprocessing methods
*/
enum Preprocess {
	NONE,
	SIMPLE_BLUR
};

/**
* image features types
*/
enum Feature {
	SIMPLE_DOM_COL
};

/**
* classification methods
*/
enum Classification {
	KNN
};